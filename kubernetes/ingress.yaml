apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: recipe-page-main
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
    - host: mmalabugin.ru
      http:
        paths:
          - path: /SimpleOmelleteRecipe
            pathType: Prefix
            backend:
              service:
                name: recipe-page-main
                port:
                  number: 80
  tls:
    - hosts:
        - mmalabugin.ru
      secretName: main-page-tls
